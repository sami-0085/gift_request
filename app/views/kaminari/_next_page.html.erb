<%# Link to the "Next" page
  - available local variables
    url:           url to the next page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<span class="next flex w-10 h-10 rounded-md hover:bg-primary border border-primary justify-center items-center">
  <%= link_to_unless current_page.last?, content_tag(:i, '', class: 'fa-solid fa-angle-right'), url, rel: 'next', remote: remote %>
</span>