<%# Link to the "First" page
  - available local variables
    url:           url to the first page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<span class="first hidden md:flex w-10 h-10 rounded-full hover:bg-cyan-600 bg-primary justify-center items-center">
  <%= link_to_unless current_page.first?, content_tag(:i, '', class: 'fa-solid fa-angles-left'), url, remote: remote %>
</span>
